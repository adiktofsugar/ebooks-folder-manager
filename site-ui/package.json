{
	"name": "site-ui",
	"version": "1.0.0",
	"description": "The ui for the generated site",
	"scripts": {
		"build": "esbuild src/index.* --outdir=dist --bundle --metafile=.meta.json --loader:.html=copy --entry-names=[dir]/[name].[hash] && esbuild-html-link",
		"lint": "echo Linting complete",
		"lint:biome": "biome check",
		"lint:ts": "tsc --noEmit",
		"fix": "biome check --fix --unsafe",
		"dev": "rsync --info=progress2 -a dist/ ~/ebooks-site/ && python -m http.server -d ~/ebooks-site"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"chalk": "^5.4.1",
		"fuzzy": "^0.1.3",
		"mobx": "^6.13.7",
		"mobx-react-lite": "^4.1.0",
		"react": "^19.1.0",
		"react-dom": "^19.1.0",
		"yaml": "^2.8.0"
	}
}
