{
  "name": "efm-ui",
  "version": "1.0.0",
  "description": "ui for site generated by efm",
  "main": "index.tsx",
  "scripts": {
    "test": "tsx --test",
    "build": "esbuild --bundle src/index.* --loader:.html=copy --outdir=../site --sourcemap",
    "start": "npm run build -- --serve",
    "watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/react": "^19.1.6",
    "@types/react-dom": "^19.1.6",
    "esbuild": "^0.25.5",
    "tsx": "^4.19.4"
  },
  "dependencies": {
    "fuzzy": "^0.1.3",
    "mobx": "^6.13.7",
    "mobx-react-lite": "^4.1.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "yaml": "^2.8.0"
  }
}
